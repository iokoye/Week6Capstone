@model Week6Capstone.Models.TaskModel

@{
    ViewData["Title"] = "Add Task";
}

<h1>Add Task</h1>

<h4>Enter Task Information</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="AddTask">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            @*<input type="hidden" name="userId" value="@Model.UserId.ID" />
            <input type="hidden" name="userEmail" value="@Model.UserId.Email" />
            <input type="hidden" name="userPassword" value="@Model.UserId.Password" />*@
            <div class="form-group">
                <label asp-for="Description" class="control-label"></label>
                <input asp-for="Description" class="form-control" />
                <span asp-validation-for="Description" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="DueDate" class="control-label"></label>
                <input asp-for="DueDate" class="form-control" />
                <span asp-validation-for="DueDate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="CompletedBy" class="control-label"></label>
                <input asp-for="CompletedBy" class="form-control" />
                <span asp-validation-for="CompletedBy" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
